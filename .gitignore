# Dependencies
node_modules/
**/node_modules/

# Build artifacts
dist/
**/dist/
*.local

# Environment
.env
.env.local
**/.env
**/.env.local

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Temporary
*.tmp
*.temp
.cache/
coverage/

# Stale duplicate of the project that shipped earlier — excluded from git
# (content lives on disk as a local backup; the canonical source is in src/ + backend/)
vue-ga4-ecommerce/

# Big local DB dump — don't commit to a public repo (contains encoded images)
carmodel_shop.sql

# Private prompts / drafts you don't want to publish
promt.txt
refinement-prompt.md

# Chrome DevTools screenshot tmp (from agent runs)
*-screenshot.png
